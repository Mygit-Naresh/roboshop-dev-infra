pipeline {
  agent {
    node {
        label 'jenkins-agent-1'
        }
}
    stages {
     stage('vpc_deploy'){
       steps {
         echo "terraform apply command going to apply"
          "terraform apply -auto-approve"
          }
      }
}

}
