pipeline {
  agent {
    node {
        label 'jenkins-agent-1'
        customWorkspace '/some/other/path'
    }
}
    stages {
     stage(vpc_deploy){
       steps {
        "terraform apply -auto-approve"
          }
      }
}

}
