pipeline {
   agent  any
  //   node {
  //       label 'jenkins-agent-1'
  //       }

    stages {
      stage('vpc_deploy'){

        steps {
           cd 01-vpc
           sh 'terraform apply -auto-approve'
             }
      }
}

}
